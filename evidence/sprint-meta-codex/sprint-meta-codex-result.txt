Sprint META-CODEX evidence — S6_generator_routes_through_codex_enforced (NEW)
==============================================================================

This is the evidence artefact that establishes the NEW backlog item being
added to test-results.json this turn. The item itself is currently
passes=false (just added). This file is the SCAFFOLD for the future
grade, not the grade.

Why this item exists (provenance):

  2026-05-22 — Marco asked the running session whether code-heavy sprint
  generation was being routed through the codex-executor agent (Codex
  GPT-5.5 xhigh) per the project's CLAUDE.md rule and the global default
  execution model. Honest audit returned: no.

  Concrete signals that proved self-execution:
    1. No codex-spawn logs anywhere under .claude/codex-logs/ or
       comparable paths in this workspace.
    2. No "Co-Authored-By: codex" trailer or comparable codex-author
       attribution in the last 5 commits (0da52ea, 0c1df8c, bc8bd93,
       23a3dfb, baf969d). Commit author is "Marco Lobo" with body text
       that reads as a single coherent Claude voice across all 5 entries.
    3. Sprints 0-3 evidence trail in evidence/sprint-{0,1,2,3} shows
       *-result.txt files with no codex-emitted log artefacts; the
       evaluator-verdict trail (sprint-0..3-verdict.txt) is intact but
       names the evaluator only, not a codex spawn.
    4. The bundled codex-executor agent in agents/codex-executor.md was
       available the whole time. Project CLAUDE.md mandates it.

  Conclusion: the orchestrator (Claude) was the generator for sprints
  0-3. The evaluator side of the generator+evaluator loop was correct
  (fresh-context grader, PASS verdicts captured). The generator side
  silently bypassed the codex-execution-default rule.

  Marco's steering: keep iterating, and add this as a structural backlog
  item — the harness should make it hard for the orchestrator to silently
  self-execute on code-heavy sprints, not rely on operator discipline.

Proposed acceptance criterion for the new item:

  S6_generator_routes_through_codex_enforced (passes=false):
    "The harness either ENFORCES or HONESTLY DISCLOSES that the
    generator side of the generator+evaluator loop runs through the
    codex-executor agent for code-heavy sprints. At minimum: (a) a
    detection signal that the orchestrator can read to know whether the
    most recent sprint pass was generated by codex (presence of a fresh
    .claude/codex-logs/*.log, or a codex-spawn telemetry file, or a
    Co-Authored-By: codex trailer on the most recent commit on the
    sprint branch); AND (b) either a verify-gate.sh enhancement that
    blocks test-results.json flips when the detection signal is missing
    on a code-heavy sprint, OR a README + CLAUDE.md addendum that names
    self-execution as a documented soft boundary and recommends
    operator practice for closing it; AND (c) a fresh-context evaluator
    grades the chosen path."

Detection signal candidates (the eventual implementer picks one or more):

  D1 — File-presence: hooks/heartbeat-stop.sh checks for any
       .claude/codex-logs/ entry newer than the last commit. If none,
       and the diff is non-trivial (>X added lines or touches hooks/,
       scripts/, bin/), warn or block.
  D2 — Commit trailer: a pre-commit hook validates that any commit
       touching hooks/, scripts/, bin/, or agents/ has a
       Co-Authored-By: codex line OR an explicit
       "self-execution-justified: ..." annotation.
  D3 — Telemetry: bin/codex-spawn.sh appends a timestamped row to
       .claude/codex-spawn.log on every Codex invocation. Tooling can
       compare against test-results.json flip timestamps to score the
       generator+evaluator loop's actual routing.
  D4 — README disclosure: explicit "known soft boundary" entry that
       names this exact failure mode and points readers at the operator-
       discipline section of CLAUDE.md.

Closes meta-trap discovered 2026-05-22 in dubai-5.

Sprint number: 6 (slotted next to evaluator-strict, since both are
generator+evaluator loop integrity work).

Category: execution-model.

Status this turn: the item is being ADDED to test-results.json as
passes=false. A future sprint (likely the next one after the in-flight
Sprint 2 evidence-gate-generalization batch) will close it through
codex-executor. That sprint will be the canonical demonstration that
the rule is now respected — i.e., codex builds the enforcement OR the
disclosure, and a fresh-context evaluator grades the work.
