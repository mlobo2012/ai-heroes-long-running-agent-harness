S6_generator_routes_through_codex_enforced

Acceptance criterion verbatim:
"The harness either ENFORCES or HONESTLY DISCLOSES that the generator side of the generator+evaluator loop runs through the codex-executor agent for code-heavy sprints. At minimum: (a) a detection signal the orchestrator can read to know whether the most recent sprint pass was generated by codex (a fresh .claude/codex-logs/*.log, or a codex-spawn telemetry file, or a Co-Authored-By: codex trailer on the most recent commit on the sprint branch); AND (b) either a verify-gate.sh enhancement that blocks test-results.json flips when the detection signal is missing on a code-heavy sprint, OR a README + CLAUDE.md addendum that names self-execution as a documented soft boundary and recommends operator practice for closing it; AND (c) a fresh-context evaluator grades the chosen path. Provenance: discovered 2026-05-22 in dubai-5 — sprints 0-3 generator was Claude not Codex, evaluator side was correct."

File-and-line citations:
- README.md:66-75 adds "Codex routing: soft boundary on the generator side", states the code-heavy routing rule, names the spawn-log and co-author detection signals, and discloses partial enforcement.
- CLAUDE.md:47-51 adds the corresponding routing rule paragraph and references the README soft-boundary section.
- hooks/verify-gate.sh:28-57 starts the codex-routing precheck, defines the code-heavy categories, code directories, TTL input, and quoted rule.
- hooks/verify-gate.sh:81-142 parses the proposed test-results.json content and detects code-heavy rows flipping from false to true.
- hooks/verify-gate.sh:145-179 gracefully handles git absence/non-repo cases and limits enforcement to changesets touching hooks/, scripts/, bin/, or agents/.
- hooks/verify-gate.sh:181-198 implements detection-signal precedence: fresh .claude/goal-state/codex-spawn-*.log first, then Co-Authored-By: codex on HEAD.
- hooks/verify-gate.sh:202-219 emits the block decision without consuming the evidence-read log.

Spot-checks:
- T1 README contains the routing rule: `grep -n 'Codex routing: soft boundary on the generator side\|Code-heavy sprint work goes through `codex-executor`' README.md` returned lines 66 and 68.
- T2 CLAUDE.md mentions the rule: `grep -n 'Codex Routing Rule and Soft Boundary\|build-eval-loop.sh\|code-heavy row flips' CLAUDE.md` returned lines 39, 47, and 51.
- T3 verify-gate.sh contains the enforcement block: `grep -n 'codex_routing_decision\|CODE_HEAVY_CATEGORIES\|Co-Authored-By: codex\|CODEX_SPAWN_TTL_SECONDS' hooks/verify-gate.sh` returned lines 28, 29, 38, 137, 182, 210, and 218.
- T4 synthetic block test: in an isolated git repo, a code-heavy flip for S6_generator_routes_through_codex_enforced with a modified hooks/touched.sh, no recent codex-spawn log, and no Co-Authored-By: codex trailer emitted `{"decision":"block","reason":"Codex routing detection failed for code-heavy sprint row(s) S6_generator_routes_through_codex_enforced: missing fresh .claude/goal-state/codex-spawn-*.log and missing Co-Authored-By: codex trailer; rule: \"code-heavy sprint work goes through codex-executor\"."}`. The evidence-read log remained non-empty.
- T5 synthetic allow test with fresh codex-spawn log: same isolated edit plus .claude/goal-state/codex-spawn-synthetic.log produced no block output and allowed the existing binding path to continue.
- T6 synthetic allow test with Co-Authored-By: codex trailer: same isolated edit, no fresh spawn log, and HEAD message containing `Co-Authored-By: codex <codex@example.com>` produced no block output and allowed the existing binding path to continue.
- T7 graceful fallback: same hook input in a non-git temporary directory exited without block output and wrote the expected stderr warning that the workspace is not a git repo.

Fresh-context evaluator:
PASS from subagent 019e4f5d-7978-73c3-bdca-a8d3a0dc8c2c. It found the verify-gate routing block satisfies the missing-signal block path, detection-signal allow paths, graceful fallback, and no evidence-log consumption on routing block.

Self-grade:
PASS. The chosen path is enforcement plus honest soft-boundary documentation: verify-gate blocks code-heavy flips when neither accepted signal is present, while README and CLAUDE.md explain what remains operator discipline.
