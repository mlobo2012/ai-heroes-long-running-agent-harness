S6_generator_evaluator_loop_documented

Acceptance criterion verbatim:
"scripts/build-eval-loop.sh OR a CLAUDE.md addendum documents the agent-driven Codex-then-evaluator handoff pattern. The codex-executor's contract is updated to require an evaluator verdict before the orchestrator flips test-results.json. Closes matrix row 9 (partial → parity)."

File-and-line citations:
- scripts/build-eval-loop.sh:6-13 documents usage, pinned telemetry, and evaluator-before-flip rule.
- scripts/build-eval-loop.sh:34-48 validates the slug and pins the spawn log to .claude/goal-state/codex-spawn-${sprint_slug}.log.
- scripts/build-eval-loop.sh:55-68 accepts a brief path or stdin and fails loud on empty briefs.
- scripts/build-eval-loop.sh:77-90 invokes bin/codex-spawn.sh, captures the log, and fails if the log is missing or empty.
- scripts/build-eval-loop.sh:93-100 prints the orchestrator's next evaluator and test-results.json steps.
- CLAUDE.md:34-43 updates the Generator -> Evaluator Loop to use scripts/build-eval-loop.sh for the Codex generator and keeps the fresh-context evaluator step before the flip.
- CLAUDE.md:47-51 adds the Codex routing rule, spawn-log verification requirement, and soft-boundary disclosure.
- agents/codex-executor.md:17-23 requires .claude/goal-state/codex-spawn-<slug>.log, requires the orchestrator to verify it, and states that codex-executor PASS is not enough without evaluator PASS.
- agents/codex-executor.md:38-40 shows the executor command and non-empty spawn-log check.

Spot-checks:
- T1 script help: `scripts/build-eval-loop.sh --help` exited 0 and printed usage plus the pinned telemetry path.
- T2 syntax/executable: `test -x scripts/build-eval-loop.sh` passed; `bash -n scripts/build-eval-loop.sh` passed.
- T3 dry-run telemetry: `scripts/build-eval-loop.sh --dry-run s7-loop-docs-and-codex-routing <brief-file>` exited 0 and wrote .claude/goal-state/codex-spawn-s7-loop-docs-and-codex-routing.log. `wc -c` reported 240 bytes, and the first line was the expected `codex exec -m gpt-5.5 -c model_reasoning_effort=xhigh ... --skip-git-repo-check` dry-run command.
- T4 CLAUDE.md script reference: `grep -n 'build-eval-loop.sh' CLAUDE.md` returned line 39.
- T5 codex-executor verify-spawn-log clause: `grep -n 'codex-spawn-<slug>\|orchestrator owns the evaluator\|test -s.*codex-spawn' agents/codex-executor.md` returned lines 17, 23, and 40.

Fresh-context evaluator:
PASS from subagent 019e4f5d-7978-73c3-bdca-a8d3a0dc8c2c. It found the wrapper, docs, executor contract, hook enforcement, and evidence files satisfied the sprint acceptance; it ran light checks including bash syntax, wrapper help, and invalid-slug validation.

Self-grade:
PASS. The wrapper codifies Codex generation with pinned telemetry and explicitly hands the evaluator step back to the orchestrator; the executor contract now requires stable spawn telemetry and evaluator PASS before test-results.json flips.
